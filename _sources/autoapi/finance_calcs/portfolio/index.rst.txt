finance_calcs.portfolio
=======================

.. py:module:: finance_calcs.portfolio

.. autoapi-nested-parse::

   Portfolio-level exposure and concentration metrics.

   All inputs are expressions over a column of position weights or
   notional dollar exposures, evaluated within the appropriate group
   (typically a ``group_by("date")``). Weights need not sum to one — the
   expressions operate on whatever is supplied.



Functions
---------

.. autoapisummary::

   finance_calcs.portfolio.gross_leverage
   finance_calcs.portfolio.gross_exposure
   finance_calcs.portfolio.net_exposure
   finance_calcs.portfolio.long_exposure
   finance_calcs.portfolio.short_exposure
   finance_calcs.portfolio.concentration
   finance_calcs.portfolio.top_n_concentration
   finance_calcs.portfolio.active_share


Module Contents
---------------

.. py:function:: gross_leverage(weights: polars.Expr) -> polars.Expr

   Sum of absolute weights — total notional / equity.

   :param weights: Position weight expression.

   :returns: Scalar gross-leverage expression.


.. py:function:: gross_exposure(weights: polars.Expr) -> polars.Expr

   Alias for ``gross_leverage`` — long + short notional.

   :param weights: Position weight expression.

   :returns: Scalar gross-exposure expression.


.. py:function:: net_exposure(weights: polars.Expr) -> polars.Expr

   Long minus short notional — signed sum of weights.

   :param weights: Position weight expression.

   :returns: Scalar net-exposure expression.


.. py:function:: long_exposure(weights: polars.Expr) -> polars.Expr

   Sum of positive weights.

   :param weights: Position weight expression.

   :returns: Scalar long-exposure expression.


.. py:function:: short_exposure(weights: polars.Expr) -> polars.Expr

   Sum of negative weights (returned as a negative number).

   :param weights: Position weight expression.

   :returns: Scalar short-exposure expression.


.. py:function:: concentration(weights: polars.Expr) -> polars.Expr

   Herfindahl-Hirschman index of normalised absolute weights.

   Computed on absolute weights normalised to sum to 1 — yields
   ``1/N`` for an equal-weight portfolio of ``N`` names and ``1.0``
   for a single-name portfolio.

   :param weights: Position weight expression.

   :returns: Scalar HHI expression in ``(0, 1]``.


.. py:function:: top_n_concentration(weights: polars.Expr, n: int = 10) -> polars.Expr

   Fraction of gross exposure held by the top ``n`` absolute weights.

   :param weights: Position weight expression.
   :param n: Number of top positions.

   :returns: Scalar expression in ``[0, 1]``.


.. py:function:: active_share(weights: polars.Expr, benchmark_weights: polars.Expr) -> polars.Expr

   Active share — ``0.5 * sum(|w - b|)``.

   :param weights: Portfolio weight expression.
   :param benchmark_weights: Benchmark weight expression aligned to ``weights``.

   :returns: Scalar active-share expression in ``[0, 1]``.


